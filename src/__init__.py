# ======================= PACKAGE INITIALIZER =======================
# This file makes the 'src' directory a Python package.
# Steps:
# 1. Optionally import key modules (e.g., from storage import data_store)
# 2. Define what should be accessible when 'src' is imported
# 3. Keep it lightweight — no heavy processing here
# Purpose: Allow other files or the Streamlit app to import functions 
# from 'src' (like src.storage or src.query_engine) smoothly.

